import styled from 'styled-components';

const StyledLeaderboard = styled.div``;

const StyledBlock = styled.div`
  border-radius: 10px;
  width: 90%;
  background-color: #ffffff;
`;

export { StyledLeaderboard, StyledBlock };
